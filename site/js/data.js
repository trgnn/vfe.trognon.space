// Machine-managed by sync-and-push.sh — albums and series only.
// Per-item info (name, subtitle, era, credits, count) is written/reconciled by the script;
// high-level curation (collections, starred) lives in curation.js, which the
// script never touches.
const VFE = {
  albums: [
    {
      slug: 'haloInfinite',
      name: 'Halo Infinite',
      subtitle: 'Campaign',
      era: 'archive',
      developer: '343 Industries',
      publisher: 'Microsoft',
      count: 26
    },
  ],

  series: [
  ]
};
