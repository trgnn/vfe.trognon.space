{
  "name": "vfe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "View From Elsewhere — build & deploy tooling",
  "scripts": {
    "blob:sync": "node --env-file-if-exists=.env tools/blob-sync.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.1"
  }
}
